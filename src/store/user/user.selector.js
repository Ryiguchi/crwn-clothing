export const selectCurrentUser = (state) => state.user.currentUser;

export const selectIsUserMenuOpen = (state) => state.user.isUserMenuOpen;

export const selectUserSettingsMenu = (state) => state.user.userSettingsMenu;
